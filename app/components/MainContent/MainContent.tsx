import React from "react";

export default function MainContent({ selectedTab }: { selectedTab: string }) {
    // TODO: Switch statement to return the appropriate page based on the selectedTab
    return (
        <></>
    )
}